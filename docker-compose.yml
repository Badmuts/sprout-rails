version: '3.1'

services:
  app:
    restart: always
    build: .
    ports:
      - 3000:3000
    volumes:
      - '.:/usr/src/app'

  db:
    restart: always
    image: postgres:9.6.5-alpine
    ports:
      - 5432:5432